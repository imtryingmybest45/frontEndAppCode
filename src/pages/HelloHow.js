function HelloHow(props){
      const { name } = props; // Destructure the prop
      return (
        <div>
            <p style={{ whiteSpace: 'pre-wrap' }}>fdsalkfdlks</p>
        </div>
      );
    }

export default HelloHow;